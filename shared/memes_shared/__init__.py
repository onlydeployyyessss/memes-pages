"""MEMES PAGES — shared domain package.

Contains database models, business services (Trend Hunter, pipeline,
scheduling, publishing, analytics) and utilities used by the API backend,
the Telegram bot and the background worker.
"""

__version__ = "1.0.0"
